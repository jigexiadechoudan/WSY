- [x] 用户相关表 (`users`, `practice_records`, `user_works` 等) 成功在数据库中创建，且模型无报错。
- [x] 数据库迁移脚本 `migrate_user_system.py` 可无报错运行并初始化默认用户数据。
- [x] `GET /api/v1/user/profile` 能成功返回包含五维数据的用户信息，每个维度分数在 0-100 之间。
- [x] 添加练习记录后，用户的总学习时长、平均准确率及五维能力会自动重新计算并更新。
- [x] 证书生成接口可成功调用，返回包含用户信息、等级、日期、二维码且美观（东方美学模板）的图片或PDF。
- [x] 整体模块 E 业务逻辑校验完备，前后端 API 可正常联调。
