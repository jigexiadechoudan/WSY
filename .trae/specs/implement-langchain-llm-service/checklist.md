* [x] DeepSeek LLM API 密钥和 URL 配置能够正常加载和调用

* [x] 成功使用 `ChatPromptTemplate` 替代原有的字符串拼接方式

* [x] 实现了对话历史记忆（通过传入不同的 session\_id，能够独立保留不同用户的多轮对话记录）

* [x] 实现了流式响应方法（支持使用 `astream` 异步产生生成内容）

* [x] 测试脚本验证通过，证实能够结合记忆进行回答以及正确输出流数据

* [x] `TASKS.md` 中的 `B-004` 相关条目已更新为已完成状态

